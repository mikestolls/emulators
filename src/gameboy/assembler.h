#pragma once

#include "defines.h"

namespace gameboy
{
	namespace assembler
	{
		int assemble_to_file(const std::string& filename)
		{
			printf("Error: gameboy assembler not implemeted");
			return -1;
		}
	}
}