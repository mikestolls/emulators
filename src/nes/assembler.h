#pragma once

#include "defines.h"

namespace nes
{
	namespace assembler
	{
		int assemble_to_file(const std::string& filename)
		{
			printf("Error: nes assembler not implemeted");
			return -1;
		}
	}
}